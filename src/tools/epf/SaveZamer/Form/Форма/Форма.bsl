&НаКлиенте
Перем ТекИдШага;

&НаКлиенте
Перем МассивШагов;

&НаКлиенте
Процедура ОбработатьШаг()
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ТекИдШага = 0;
	ПодключитьОбработчикОжидания("ОбработатьШаг",1);
КонецПроцедуры


МассивШагов = Новый Массив;
МассивШагов.Добавить("МаксимизироватьОкно");
МассивШагов.Добавить("ЗапуститьКонфигуратор");